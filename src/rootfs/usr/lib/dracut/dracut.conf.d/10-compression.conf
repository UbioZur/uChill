# With this change any regenerated initramfs, either at runtime when configured by a user, 
# or at build time for downstreams like hwe, bazzite, bluefin/aurora, the resulting initramfs 
# image will be compressed with zstd, saving a bit of space.
compress="zstd"
